spring:
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: CONFIG-SERVER
      label: master
  application:
    name: order-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/